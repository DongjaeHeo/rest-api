flask==2.1.3
flask-smorest==0.36.0  # as of my last update, you might need to adjust if newer versions are available
python-dotenv==0.19.0  # as of my last update, adjust if needed
sqlalchemy==1.4.25  # this version is known to be compatible with Flask 2.1.3
flask-sqlalchemy==2.5.1
flask-jwt-extended==4.3.1  # as of my last update, adjust if needed
passlib==1.7.4  # as of my last update, adjust if needed
flask-migrate==3.1.0
gunicorn==20.1.0  # as of my last update, adjust if needed
psycopg2==2.9.1
requests==2.26.0
rq==1.10.0
redis==3.5.3
flask_cors==3.0.10
